- [ ] Build an extension bundle using esbuild, per the docs for [VSCode: Bundling Extensions](https://code.visualstudio.com/api/working-with-extensions/bundling-extension)

- [ ] Add a config option to disable the automatic colour change, so that it can be disabled per workspace in `.vscode/settings.json`.
